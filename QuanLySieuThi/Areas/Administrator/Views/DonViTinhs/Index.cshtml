@model IEnumerable<QuanLySieuThi.Models.Entities.DonViTinh>

@{
    ViewData["Title"] = "Đơn vị tính";
    Layout = "~/Areas/Administrator/Views/Shared/_Layout.cshtml";
}

<h2 style="color:blue;font-style:oblique">Danh sách đơn vị tính</h2>

<p>
    <i class="fa fa-newspaper-o"></i>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table" border="1">
    <tr style="color:red;font-size:15px;font-style:oblique">
        <th>
            @Html.DisplayNameFor(model => model.MaDVT)
        </th>
        <th>
            s
            @Html.DisplayNameFor(model => model.Ten)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaDVT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ten)
            </td>
            <td>
                <i class="fa fa-edit" style="color:blue;"></i>
                @Html.ActionLink("Thay đổi", "Edit", new { id = item.MaDVT }) |
                <i class="fa fa-deaf" style="color:blue;"></i>
                @Html.ActionLink("Chi tiết", "Details", new { id = item.MaDVT }) |
                <i class="fa fa-remove" style="color:blue;"></i>
                @Html.ActionLink("Xóa", "Delete", new { id = item.MaDVT })
            </td>
        </tr>
    }

</table>
